//Definimos el Array
const Array = [40, 50, 60, 70, 80, 90];
//Mostramos el array antes de la eliminacion
console.log("Array antes de la eliminacion: ", Array);
    //Utiilizamos pop para eliminar el ultimo elemento del array
    Array.pop();
    //Mostrammos el array despues de la eliminacion
    console.log("Array despues de la eliminacion: ", Array);
